// src/pages/Rack.jsx
// Este componente fue reemplazado por CalendarioRack.
// Se mantiene solo para compatibilidad con imports antiguos.
import { Navigate } from "react-router-dom";

export default function Rack() {
  return <Navigate to="/rack" replace />;
}